# DSA Sheet Client

React + Vite frontend for the DSA Sheet Platform.

Run locally from the repository root:

```bash
npm run dev
```

The dev server proxies `/api` requests to the Express API on port `5001`.
